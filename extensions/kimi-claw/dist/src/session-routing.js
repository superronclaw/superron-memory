import{asTrimmedNonEmptyString as asString}from"./utils/text.js";export const SESSION_KEY_NAMESPACE="agent";export const DEFAULT_AGENT_SESSION_SEGMENT="main";const MAIN_ROUTE_SEGMENT="main",DEFAULT_ISOLATED_CHANNEL_SEGMENT="kimi-claw",normalizeSessionKeySegment=e=>{const n=asString(e);if(!n)return"unknown";const t=n.toLowerCase().replace(/[^a-z0-9._-]+/g,"-").replace(/^-+/,"").replace(/-+$/,"");return t.length>0?t.slice(0,128):"unknown"};export const buildMainSessionKey=(e="main")=>`agent:${normalizeSessionKeySegment(e)}:main`;export const MAIN_SESSION_KEY=buildMainSessionKey();export const buildIsolatedSessionKey=e=>{const n=normalizeSessionKeySegment(e.target),t=normalizeSessionKeySegment(e.agentSessionSegment??"main"),i=normalizeSessionKeySegment(e.channelSegment??"kimi-claw"),o=asString(e.accountId);return`agent:${t}:${i}${o?`:${normalizeSessionKeySegment(o)}`:""}:direct:${n}${void 0!==e.threadId&&null!==e.threadId?`:thread:${normalizeSessionKeySegment(String(e.threadId))}`:""}`};